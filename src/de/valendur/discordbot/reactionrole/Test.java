package de.valendur.discordbot.reactionrole;

import java.util.HashMap;
import java.util.Map;

public class Test {

	 
	
	public static void main(String[] args) {
		
		int rank = 2;
		HashMap<Integer, String> map = new HashMap<>();
		
		String roleID = map.get(rank);
		if (roleID != null) {
			//assign rank
		}
		if (rank == 5) {
			// extra stuff
		}
		
		
		
	}
}
